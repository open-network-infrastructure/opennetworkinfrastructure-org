# Install the apt prerequisites
addons:
  apt:
    packages:
      - python-pygments

# Clean and don't fail
install:
  - wget https://github.com/gohugoio/hugo/releases/download/v0.54.0/hugo_0.54.0_Linux-64bit.deb -O /tmp/hugo.deb && sudo dpkg -i /tmp/hugo.deb
  - rm -rf public || exit 0
  - mkdir -p bin && curl "%{filename_effective}" -LO $(curl -s https://api.github.com/repos/tdewolff/minify/releases | grep browser_download_url | grep 'linux_amd64[.]tar[.]gz' | head -n 1 | cut -d '"' -f 4) | tar xz -C bin

# Build the website
script:
  - hugo
  - ./bin/minify --all -r  -o ./public ./public

# Deploy to GitHub pages
deploy:
  provider: pages
  repo: open-network-infrastructure/open-network-infrastructure.github.io
  name: Roberto, the ONIA automaton
  target-branch: master
  skip_cleanup: true
  keep-history: true
  github_token: $GITHUB_TOKEN
  local_dir: public
  on:
    branch: master
